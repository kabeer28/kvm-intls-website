# Supabase
VITE_SUPABASE_URL=your-supabase-project-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# You'll need to set up a Supabase project and create a 'contact_submissions' table with these columns:
# - id (uuid, primary key)
# - created_at (timestamp with time zone)
# - first_name (text)
# - last_name (text)
# - email (text)
# - phone (text, nullable)
# - company (text, nullable)
# - message (text)
